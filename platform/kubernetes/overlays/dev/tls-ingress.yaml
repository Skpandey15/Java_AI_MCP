apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: online-interview-tls
spec:
  secretName: online-interview-tls
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-staging
  dnsNames:
    - interview.dev.example.com
    - api.interview.dev.example.com
    - auth.dev.example.com
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: online-interview
  annotations:
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
spec:
  ingressClassName: nginx
  tls:
    - secretName: online-interview-tls
      hosts:
        - interview.dev.example.com
        - api.interview.dev.example.com
        - auth.dev.example.com
  rules:
    - host: interview.dev.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service: {name: web-ui, port: {number: 80}}
    - host: api.interview.dev.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service: {name: interview-orchestrator, port: {number: 8080}}
    - host: auth.dev.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service: {name: keycloak, port: {number: 8080}}
